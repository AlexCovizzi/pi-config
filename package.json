{
  "private": true,
  "type": "module",
  "scripts": {
    "check": "biome check . && tsc --noEmit",
    "lint": "biome lint .",
    "format": "biome format . --write .",
    "fix": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "postinstall": "node scripts/symlink.cjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2"
  }
}
